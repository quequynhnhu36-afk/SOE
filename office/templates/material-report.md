# Template: Work Report

## 关于 `[事项]` 工作情况的汇报

### 一、基本情况

围绕 `[工作背景或上级要求]`，`[单位名称]` 结合 `[业务场景]`，组织开展 `[具体工作]`。截至 `[日期]`，已完成 `[已完成事项]`，覆盖 `[范围或对象]`。

### 二、主要进展

一是 `[进展一]`。`[事实、数据、案例]`

二是 `[进展二]`。`[事实、数据、案例]`

三是 `[进展三]`。`[事实、数据、案例]`

### 三、存在问题

1. `[问题一]`，主要表现为 `[具体表现]`。
2. `[问题二]`，主要表现为 `[具体表现]`。

### 四、下一步安排

1. `[措施一]`，由 `[责任单位]` 牵头，于 `[时间]` 前完成 `[交付物]`。
2. `[措施二]`，由 `[责任单位]` 牵头，形成 `[机制或成果]`。

### 五、需协调事项

请协调 `[单位或部门]` 支持 `[事项]`。

